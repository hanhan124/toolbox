# 2026-06-26

## 天气组件开发
- 在首页 logo 下方新增天气显示组件 (`src/components/WeatherWidget.tsx`)
- 天气数据源: Open-Meteo API (无需 API Key, 全球可用)
- 定位策略: 双重 fallback — ipapi.co (HTTPS, 国际) → ip-api.com (HTTP, 国内)
- 更新 Tauri CSP (`tauri.conf.json`) 增加 `api.open-meteo.com`, `ipapi.co`, `ip-api.com` 到 connect-src
- 修改 `Home.tsx` 在 brand 区域下方插入 WeatherWidget
- 在 `pages.css` 新增 `.weather-widget` 系列样式, 匹配现有 macOS 风格
- 组件特性: 加载态 spinner / 错误态可点击重试 / 成功态显示图标+温度+天气+城市 / 30分钟自动刷新
- 通过 tsc + eslint + vite build 全部检查

## 全项目 Code Review
- 完成 5 维度全面审查, 生成 `docs/CODE_REVIEW.md` 详细报告
- 综合评分 8.1/10, 0 个 P0 阻塞性 bug, 3 个 P1 问题:
  1. TIFF PowerShell 路径注入风险 (需完善转义)
  2. ConvertOptions.tsx 字符串布尔值反模式
  3. compareVersions 在 Sidebar 和 UpdateNotification 中重复定义
- 亮点: macOS 双主题体系、Rust VBS 安全校验、React lazy 分包、Loading 全覆盖

## Code Review 全部修复 (P1~P3)
### P1 高优先级 (3/3)
- **TIFF PowerShell 路径转义**: `escapePsSingleQuoted` → `escapePsString`，新增 `$`、反引号、`()` 转义
- **ConvertOptions 字符串布尔值**: `bold`/`italic`/`addLabel` 从 `"true"/"false"/"1"` 改为 boolean，select 使用 `value={bool ? "1" : "0"}` 模式
- **compareVersions 去重**: 抽取为 `src/lib/version.ts`，Sidebar 和 UpdateNotification 改为 import 共享

### P2 建议修复 (4/4)
- **getpageTitle → getPageTitle**: 修正 tools.tsx 函数命名 + App.tsx 调用
- **qPCR 列宽优化**: 移除 `eachCell({ includeEmpty: true })`，仅遍历有值单元格
- **ip-api.com → HTTPS**: WeatherWidget 和 CSP 中 `http://ip-api.com` 改为 `https://ip-api.com`
- **WeatherWidget 缓存**: 新增 sessionStorage 30 分钟缓存，二次打开即显，后台静默刷新

### P3 可选优化 (4/4)
- **Sidebar 组件拆分**: 抽取 `src/components/AboutModal.tsx`，Sidebar 从 180+ 行缩减至 100 行
- **天气图标 SVG 化**: emoji → lucide-react (Sun/Cloud/CloudRain/CloudSnow/CloudLightning/CloudFog/CloudDrizzle/CloudSun/MapPin)，跨平台一致渲染
- **Rust 便携更新路径限制**: `portable_self_update` 增加 temp 目录前缀校验
- **窗口最小宽度**: 360px → 380px

### 验证
- ✅ tsc --noEmit: 0 errors
- ✅ eslint: 0 errors, 3 pre-existing warnings
- ✅ vite build: 成功

## 天气组件 Bug 修复
- 问题: 天气获取失败。定位到两个 API 均不可用：
  1. ipapi.co 免费层已关闭（需付费），返回 "sign up for a paid plan"
  2. ip-api.com 免费层不支持 HTTPS，强制 HTTPS 后返回 `{"status":"fail"}`
- 修复:
  - 策略 1: ipapi.co → `ipwho.is` (HTTPS, 免费, 无需 Key, 返回 lat/lon/city)
  - 策略 2: ip-api.com 恢复 HTTP (免费层限制)
  - CSP 更新: `https://ipapi.co` → `https://ipwho.is`, `https://ip-api.com` → `http://ip-api.com`
- 全链路验证: ipwho.is ✅ ip-api.com HTTP ✅ Open-Meteo ✅
